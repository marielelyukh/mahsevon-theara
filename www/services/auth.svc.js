;(function () {
  'use strict';

  angular.module('service.authSvc', []).factory('authSvc', authSvc);

  authSvc.$inject = ['$localStorage', 'http', 'url'];

  function authSvc($localStorage, http, url) {
    var model = {

    };


  }
})();
